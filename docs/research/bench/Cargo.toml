[package]
name = "cityg-bench"
version = "0.0.0"
edition = "2024"
publish = false
description = "Micro-benchmarks behind the CPU figures of docs/research/grands-groupes-2026-09-25.md"

[dependencies]
cityg-core = { path = "../../../crates/cityg-core" }
cityg-pqc = { path = "../../../crates/cityg-pqc" }
rand_chacha = "0.3"
rand_core = "0.6"

# Standalone crate, outside the repository's workspace.
[workspace]
