[package]
name = "cityg-server"
version = "0.2.0"
license = "MIT"
authors = ["Sabri Haddouche"]
edition.workspace = true
description = "City-G v0.2 delivery service: rooms, their ordered log, journal and storage"

[dependencies]
cityg-core = { path = "../cityg-core" }
ciborium = { version = "0.2", features = ["std"] }
thiserror = "2"

[dev-dependencies]
rand_chacha = "0.3"
rand_core = "0.6"
tempfile = "3.24"
